<%=flash[:notice] %>
<!-- 投稿内容 -->
    <div class="container">
    <div class="row">
      <div class="col-lg-4">
        </div>
        <div class="col-lg-8">
            <h3>Book detail</h3>
        <table class="table">
        <thead>
        <tr>
            <th>Title</th>
            <th>Opinion</th>
        </tr>
        </thead>
        </table>
      </div>
    </div>
    </div>

<!-- 繰り返し表示 -->
<% @books.each do |book| %>
<tr>
<td><%= book.title %></td>
<td><%= book.body %></td>
<!-- 各種リンク -->
<td><%= link_to "Show","/books/#{book.id}"%></td>
</tr>
<% end %>
</table>
</div>
</div>
</div>
<!-- 投稿機能-->
<%= render 'books/book',book: @book,user: @user %>