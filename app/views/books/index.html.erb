<%=flash[:notice] %>
<!-- 投稿内容 -->
    <div class="container">
    <div class="row">
      <div class="col-lg-4">
        </div>
        <div class="col-lg-8">
            <h3>Book detail</h3>
        <table class="table">
        <thead>
        <tr>
            <th>Title</th>
            <th>Opinion</th>
        </tr>

        <!-- 繰り返し表示 -->
        <% @books.each do |book| %>
        <tr>
            <td>< <%= @user.name %></td>
            <td><%= book.title %></td>
            <td><%= book.body %></td>
            <td><%= link_to "Show","/books/#{book.id}"%></td>
        </tr>
        </thead>
        </table>
      </div>
    </div>
    </div>
<% end %>

<!-- 投稿機能-->
<%= render 'books/book',book: @book,user: @user %>